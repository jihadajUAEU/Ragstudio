# Quick Task 260516-n1c: Fix Task 5 blocker

## Goal

Retain the `lexical_expanded_exact` score boost after duplicate candidate fusion when the winning candidate is semantic/vector but the merged duplicate evidence contains lexical-expanded signals.

## Tasks

1. Update `backend/src/ragstudio/services/retrieval_evidence.py` so scoring detects lexical-expanded evidence from the direct retrieval pass, merged metadata `retrieval_passes`, or merged `match_features.lexical_expanded`.
2. Add a focused regression in `backend/tests/test_retrieval_orchestrator.py` for a higher-base semantic/vector candidate merged with a lower-base lexical-expanded duplicate on the same chunk.
3. Run the requested pytest and ruff commands, then commit with `fix: retain lexical boost after dedupe`.
