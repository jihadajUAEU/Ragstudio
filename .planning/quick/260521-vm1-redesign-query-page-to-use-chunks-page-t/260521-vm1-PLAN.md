# Quick Task 260521-vm1: Redesign query page to use chunks page template with expanded query and retrieval tuning controls

## Goal

Redesign `frontend/src/features/query/query-page.tsx` so `/query` follows the compact Chunks page structure:

- top command panel with horizontal document and variant selectors
- visible query controls for question, limit, answer mode, tuning, refresh, and run
- expandable retrieval tuning options with budgets and search-weight state
- full-width results section below, preserving answer, evidence, pathway, and trace inspection

## Constraints

- Keep scope to the query page unless compilation forces a nearby adjustment.
- Preserve existing query behavior and API payload shape.
- Do not touch unrelated dirty backend or chunks-page work.
- Verify with frontend build or a focused frontend check.
