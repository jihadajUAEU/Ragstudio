# Quick Task 260522-o6u: Redesign document page at /documents

## Goal

Redesign the Documents page while preserving existing upload, vision metadata,
document table, job table, warning inspection, and mutation behavior.

## Scope

- Rework `frontend/src/features/documents/documents-page.tsx`.
- Keep the implementation inside the existing React/Tailwind patterns.
- Reuse local UI primitives and design tokens.
- Verify with focused frontend tests and browser inspection where possible.

## Plan

1. Inspect the current Documents page structure, tests, shared UI primitives, and
   design tokens.
2. Replace the top-level layout with a status-first operational workspace:
   concise header actions, metric strip, upload panel, and table panels.
3. Improve upload and jobs presentation without changing API calls or labels that
   tests depend on.
4. Run the Documents page test file and build or lint checks as time permits.
5. Record a summary for the quick task.
