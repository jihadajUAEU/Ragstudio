---
phase: 05-launch-hardening-and-domain-release
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/meet/Documents/ragstudio-site/package.json
  - /Users/meet/Documents/ragstudio-site/docs/launch/cloudflare-pages.md
  - /Users/meet/Documents/ragstudio-site/docs/launch/release-proof.json
  - /Users/meet/Documents/ragstudio-site/scripts/check-release-proof.mjs
autonomous: true
requirements:
  - SITE-02
  - SITE-03
user_setup:
  - Cloudflare account access may be required to connect the Git repository and custom domain in the dashboard.
must_haves:
  truths:
    - "Decision coverage: D-01, D-02, D-03: `https://ragstudio.dev` is the only official launch URL; Pages preview URLs are prelaunch only."
    - "Decision coverage: D-04, D-05, D-06, D-07: Cloudflare Pages project is `ragstudio-site`, production branch is `main`, build command is `npm run build`, output directory is `dist`, and runtime env vars are not required."
    - "The release proof records whether Git remote, Pages project, production branch, build command, output directory, preview URL, production deployment, and `ragstudio.dev` domain are verified."
    - "The phase does not mark launch complete from a `.pages.dev` preview URL alone."
  artifacts:
    - path: /Users/meet/Documents/ragstudio-site/docs/launch/cloudflare-pages.md
      provides: Human/operator setup and verification steps for Cloudflare Pages and `ragstudio.dev`
      contains: "ragstudio.dev"
    - path: /Users/meet/Documents/ragstudio-site/docs/launch/release-proof.json
      provides: Structured release proof consumed by later launch checks
      contains: "ragstudio-site"
    - path: /Users/meet/Documents/ragstudio-site/scripts/check-release-proof.mjs
      provides: Fail-closed validation for the structured release proof fields
      contains: "ragstudio.dev"
  key_links:
    - from: /Users/meet/Documents/ragstudio-site/docs/launch/release-proof.json
      to: /Users/meet/Documents/ragstudio-site/scripts/check-release-proof.mjs
      via: structured release proof validation
      pattern: "official_domain"
---

<objective>
Create the Cloudflare Pages and `ragstudio.dev` release proof foundation.

Purpose: Make deployment state explicit before adding launch gates or public links.
Output: `ragstudio-site` has operator docs and structured release proof that
can verify the Pages project, Git branch, build settings, and custom domain.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/05-launch-hardening-and-domain-release/05-CONTEXT.md
@.planning/phases/05-launch-hardening-and-domain-release/05-UI-SPEC.md
@.planning/phases/05-launch-hardening-and-domain-release/05-RESEARCH.md
@.planning/phases/05-launch-hardening-and-domain-release/05-PATTERNS.md
@/Users/meet/Documents/ragstudio-site/package.json
</context>

<threat_model>
Assets: public launch URL, Cloudflare Pages project settings, Git deployment
integrity, release proof, and the static-only public site boundary.
Threats: launch marked complete from a preview URL, wrong production branch,
wrong build output directory, missing Git remote, unrecorded manual dashboard
configuration, runtime env leakage, stale domain/DNS state, or impossible
verification because Cloudflare credentials are absent locally.
Mitigations: structured release proof with required fields, fail-closed proof
checker, operator docs for dashboard steps, explicit `main`/`npm run build`/`dist`
assertions, no runtime env vars, and a `ragstudio.dev` launch-complete gate.
Residual risk: actual domain activation may require user dashboard action; record
manual status until live verification passes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Cloudflare Pages launch documentation</name>
  <files>/Users/meet/Documents/ragstudio-site/docs/launch/cloudflare-pages.md</files>
  <read_first>/Users/meet/Documents/ragstudio-site/package.json, .planning/phases/05-launch-hardening-and-domain-release/05-CONTEXT.md, .planning/phases/05-launch-hardening-and-domain-release/05-RESEARCH.md</read_first>
  <action>Create `docs/launch/cloudflare-pages.md` in the site repo. Document project name `ragstudio-site`, production branch `main`, build command `npm run build`, output directory `dist`, no runtime env vars, preview URL as prelaunch-only, official domain `https://ragstudio.dev`, and dashboard steps to add `ragstudio.dev` under Workers & Pages > `ragstudio-site` > Custom domains. Include a note that the local checkout currently must verify a Git remote before Cloudflare Git integration can be complete.</action>
  <verify>test -f /Users/meet/Documents/ragstudio-site/docs/launch/cloudflare-pages.md && rg "ragstudio.dev|npm run build|dist|main|ragstudio-site" /Users/meet/Documents/ragstudio-site/docs/launch/cloudflare-pages.md</verify>
  <acceptance_criteria>
    - `cloudflare-pages.md` contains `ragstudio-site`
    - `cloudflare-pages.md` contains `production branch: main` or equivalent exact language
    - `cloudflare-pages.md` contains `npm run build`
    - `cloudflare-pages.md` contains `dist`
    - `cloudflare-pages.md` contains `https://ragstudio.dev`
    - `cloudflare-pages.md` states Pages preview URLs do not count as public launch
  </acceptance_criteria>
  <done>Cloudflare Pages setup and domain proof path is documented for execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured release proof and fail-closed checker</name>
  <files>/Users/meet/Documents/ragstudio-site/docs/launch/release-proof.json, /Users/meet/Documents/ragstudio-site/scripts/check-release-proof.mjs, /Users/meet/Documents/ragstudio-site/package.json</files>
  <read_first>/Users/meet/Documents/ragstudio-site/package.json, /Users/meet/Documents/ragstudio-site/scripts/check-static-boundary.mjs, .planning/phases/05-launch-hardening-and-domain-release/05-VALIDATION.md</read_first>
  <action>Add `docs/launch/release-proof.json` with fields for `project_name`, `production_branch`, `build_command`, `output_directory`, `runtime_environment_variables_required`, `preview_url`, `official_domain`, `domain_status`, `git_remote_verified`, `pages_project_verified`, and `production_deployment_verified`. Add `scripts/check-release-proof.mjs` that reads this JSON and exits non-zero unless project name is `ragstudio-site`, production branch is `main`, build command is `npm run build`, output directory is `dist`, runtime env vars are false, official domain is `https://ragstudio.dev`, and domain status is `active` only when production deployment is verified. Add npm script `check:release-proof`.</action>
  <verify>cd /Users/meet/Documents/ragstudio-site && npm run check:release-proof</verify>
  <acceptance_criteria>
    - `package.json` has script `check:release-proof`
    - `release-proof.json` contains `"official_domain": "https://ragstudio.dev"`
    - `release-proof.json` contains `"project_name": "ragstudio-site"`
    - `check-release-proof.mjs` exits non-zero if `official_domain` is changed away from `https://ragstudio.dev`
    - `npm run check:release-proof` exits 0 with the committed proof placeholder state or a clearly documented `pending` state accepted by the checker
  </acceptance_criteria>
  <done>Release proof is machine-checkable before final launch.</done>
</task>

<task type="auto">
  <name>Task 3: Verify local static build still works with release proof files</name>
  <files>/Users/meet/Documents/ragstudio-site/package.json, /Users/meet/Documents/ragstudio-site/docs/launch/release-proof.json</files>
  <read_first>/Users/meet/Documents/ragstudio-site/package.json, /Users/meet/Documents/ragstudio-site/scripts/check-release-proof.mjs</read_first>
  <action>Run and record the existing static checks after adding release proof files. If generated build artifacts change, do not commit `dist` unless it is already tracked by the site repo. Keep the site static-only and do not add runtime env vars or backend endpoints.</action>
  <verify>cd /Users/meet/Documents/ragstudio-site && npm run check:static && npm run lint && npm test && npm run build && npm run check:release-proof</verify>
  <acceptance_criteria>
    - `npm run check:static` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0
    - `npm run build` exits 0
    - `npm run check:release-proof` exits 0
    - No `VITE_API_`, `RAGSTUDIO_`, `/api`, `/upload`, `/auth`, or `fetch("http` patterns are introduced in `src`
  </acceptance_criteria>
  <done>Deployment proof foundation preserves the Phase 04 static site behavior.</done>
</task>

</tasks>

<verification>
- [ ] `cd /Users/meet/Documents/ragstudio-site && npm run check:static`
- [ ] `cd /Users/meet/Documents/ragstudio-site && npm run lint`
- [ ] `cd /Users/meet/Documents/ragstudio-site && npm test`
- [ ] `cd /Users/meet/Documents/ragstudio-site && npm run build`
- [ ] `cd /Users/meet/Documents/ragstudio-site && npm run check:release-proof`
</verification>

<success_criteria>
- Cloudflare Pages setup is documented with exact project/branch/build/output/domain values
- Release proof exists and is machine-checkable
- `ragstudio.dev` is the only official launch URL
- Preview URLs remain prelaunch-only
- Static-only boundary still passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-hardening-and-domain-release/05-01-SUMMARY.md`
</output>
