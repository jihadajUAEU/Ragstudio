---
phase: 06-app-evidence-viewer-and-runtime-trust-banner
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - frontend/src/features/evidence/evidence-viewer.tsx
  - frontend/src/features/chunks/chunk-inspector.tsx
  - frontend/src/features/graph/graph-page.tsx
  - frontend/tests/chunk-inspector.test.tsx
  - frontend/tests/graph-page.test.tsx
  - frontend/tests/app-shell.test.tsx
  - frontend/tests/query-page.test.tsx
autonomous: true
requirements:
  - APP-UI-01
  - APP-UI-02
user_setup:
  - Optional visual/mobile verification needs the frontend dev server running for browser inspection.
must_haves:
  truths:
    - "Decision coverage: D-14: Chunk Inspector uses the same shared evidence viewer and normalized input as Query."
    - "Decision coverage: D-15: missing graph context uses diagnostics when graph is disabled, pending, or failed; otherwise it says `No graph relationship recorded for this evidence`."
    - "Decision coverage: D-16: unmatched reranker context is labelled `Run-level reranker summary; not source-specific`."
    - "Decision coverage: D-17: missing parser warning, quality status, source location, and related evidence fields show explicit `Not recorded` states."
    - "Decision coverage: D-18: viewer route links show available actions plus disabled/unavailable labels such as `Document link not recorded`."
    - "Phase 6 remains local Studio UI only: no report export, no new routes, no graph materialization, no full PDF preview."
  artifacts:
    - path: frontend/src/features/chunks/chunk-inspector.tsx
      provides: Chunk Inspector evidence entry point
      contains: "Inspect evidence"
    - path: frontend/src/features/evidence/evidence-viewer.tsx
      provides: Graph/missing-state/link behavior shared by Query and Chunks
      contains: "No graph relationship recorded for this evidence"
    - path: frontend/tests/chunk-inspector.test.tsx
      provides: Chunk evidence viewer coverage
      contains: "chunk-1"
  key_links:
    - from: frontend/src/features/chunks/chunk-inspector.tsx
      to: frontend/src/features/evidence/evidence-viewer.tsx
      via: ChunkOut normalizer
      pattern: "Inspect evidence"
---

<objective>
Complete evidence viewer integration for Chunk Inspector and cross-surface trust polish.

Purpose: Make the shared viewer useful from both required entry points, explain
missing graph/reranker/parser/quality context honestly, and close the
accessibility/mobile verification gaps for Phase 6.
Output: Chunk Inspector rows open the same evidence drawer, graph and missing
states are explicit, route links are available or labelled unavailable, and the
targeted Phase 6 suite plus build pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-SPEC.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-CONTEXT.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-UI-SPEC.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-RESEARCH.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-VALIDATION.md
@.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-PATTERNS.md
@frontend/src/features/evidence/evidence-viewer.tsx
@frontend/src/features/chunks/chunk-inspector.tsx
@frontend/src/features/graph/graph-page.tsx
@frontend/src/api/generated.ts
@frontend/tests/chunk-inspector.test.tsx
@frontend/tests/graph-page.test.tsx
</context>

<threat_model>
Assets: chunk evidence integrity, graph relationship context, route navigation
truth, and accessibility of modal surfaces.
Threats: chunk and query viewers drifting apart, graph disabled/pending state
rendering as blank, missing evidence fields being hidden, enabled-looking links
without context, mobile controls clipped in full-screen sheet, and final build
passing while Phase 6 behavior lacks coverage.
Mitigations: shared viewer normalizer, diagnostics-derived graph explanation,
explicit `Not recorded` and unavailable labels, disabled non-links for missing
context, targeted tests for Query/Chunks/AppShell/Graph, and final build.
Residual risk: real pixel-level mobile verification may need a running dev
server; if unavailable, record this as residual manual verification in SUMMARY.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Chunk Inspector evidence entry point</name>
  <files>frontend/src/features/chunks/chunk-inspector.tsx, frontend/src/features/evidence/evidence-viewer.tsx, frontend/tests/chunk-inspector.test.tsx</files>
  <read_first>frontend/src/features/chunks/chunk-inspector.tsx, frontend/src/features/evidence/evidence-viewer.tsx, frontend/src/api/generated.ts, frontend/tests/chunk-inspector.test.tsx, .planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-CONTEXT.md</read_first>
  <action>Update `ChunkCard` to render an explicit `Inspect evidence` button. Add a `ChunkOut` normalizer that maps `chunk.id`, `document_id`, `text`, `source_location`, `metadata`, `runtime_profile_id`, `retrieval_explain`, `relationship_refs`, and raw chunk payload into the shared evidence input. Preserve existing chunk card text, badges, retrieval explain panel, source location JSON, and metadata JSON. Do not make the whole card clickable.</action>
  <verify>cd frontend && npm test -- --run chunk-inspector.test.tsx</verify>
  <acceptance_criteria>
    - Chunk result card renders a button named `Inspect evidence`
    - Clicking chunk `Inspect evidence` opens `Evidence details`
    - Viewer identifies selected chunk id `chunk-1`
    - Viewer shows exact text `Book 1, Hadith 1`
    - Viewer lists relationship ref `neighbor: chunk-2`
    - Existing retrieval explain assertions still pass
  </acceptance_criteria>
  <done>Chunk Inspector is a first-class evidence viewer entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Complete graph, missing-state, and route-link behavior</name>
  <files>frontend/src/features/evidence/evidence-viewer.tsx, frontend/src/features/graph/graph-page.tsx, frontend/tests/chunk-inspector.test.tsx, frontend/tests/query-page.test.tsx, frontend/tests/graph-page.test.tsx</files>
  <read_first>frontend/src/features/evidence/evidence-viewer.tsx, frontend/src/features/graph/graph-page.tsx, frontend/tests/graph-page.test.tsx, .planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-UI-SPEC.md</read_first>
  <action>Enhance the shared evidence viewer to accept diagnostics or a graph-unavailable detail. If evidence relationship refs exist, list them in `Graph context`. If refs are absent and diagnostics indicate graph disabled, pending, or failed, show that diagnostics reason. If diagnostics is healthy and refs are absent, show `No graph relationship recorded for this evidence`. Ensure parser warnings, quality status, source location, and document links show explicit `Not recorded` or unavailable labels when missing. Render route actions for existing Studio surfaces: `Open Documents`, `Open Chunks`, `Open Query`, `Open Graph`, and `Open Diagnostics`; if context is missing, render disabled/unavailable text such as `Document link not recorded` rather than an active broken link.</action>
  <verify>cd frontend && npm test -- --run chunk-inspector.test.tsx query-page.test.tsx graph-page.test.tsx</verify>
  <acceptance_criteria>
    - Viewer lists relationship refs when selected evidence contains them
    - Viewer shows graph diagnostics reason when graph capability is disabled or graph projection is pending
    - Viewer shows `No graph relationship recorded for this evidence` when graph is healthy but no refs exist
    - Viewer shows `Parser warnings not recorded`
    - Viewer shows `Quality policy not recorded`
    - Viewer shows `Source location not recorded`
    - Viewer shows `Document link not recorded` for missing document context
    - Available route actions include `Open Chunks`, `Open Graph`, and `Open Diagnostics`
  </acceptance_criteria>
  <done>Evidence drawer explains missing context honestly and routes to existing surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 6 accessibility, responsive, and build verification</name>
  <files>frontend/tests/app-shell.test.tsx, frontend/tests/query-page.test.tsx, frontend/tests/chunk-inspector.test.tsx, frontend/src/components/runtime-trust.tsx, frontend/src/features/evidence/evidence-viewer.tsx</files>
  <read_first>frontend/tests/app-shell.test.tsx, frontend/tests/query-page.test.tsx, frontend/tests/chunk-inspector.test.tsx, .planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-VALIDATION.md, .planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-UI-SPEC.md</read_first>
  <action>Add or tighten tests so the runtime trust panel and evidence drawer both close on Escape, restore focus to their trigger, expose `role="dialog"` and `aria-modal="true"`, and keep provider test result text in a polite status region or equivalent accessible status text. Ensure mobile/full-screen sheet classes include fixed viewport positioning and bounded overflow; ensure desktop drawer classes anchor to the right and keep content scrollable. Do not add screenshots or report export. Run the targeted Phase 6 suite and `npm run build` from `frontend/`.</action>
  <verify>cd frontend && npm test -- --run app-shell.test.tsx query-page.test.tsx chunk-inspector.test.tsx graph-page.test.tsx settings-page.test.tsx && npm run build</verify>
  <acceptance_criteria>
    - Runtime trust panel has `role="dialog"` and `aria-modal="true"`
    - Evidence viewer has `role="dialog"` and `aria-modal="true"`
    - AppShell test proves trust panel Escape close restores focus to the trust chip
    - Query or Chunk test proves evidence drawer Escape close restores focus to `Inspect evidence`
    - Provider result text is exposed as `role="status"` or `aria-live="polite"`
    - Evidence drawer container includes classes or styles for mobile full-screen behavior and bounded overflow
    - `npm test -- --run app-shell.test.tsx query-page.test.tsx chunk-inspector.test.tsx graph-page.test.tsx settings-page.test.tsx` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Phase 6 UI behavior is accessible, responsive, and build-verified.</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm test -- --run app-shell.test.tsx query-page.test.tsx chunk-inspector.test.tsx graph-page.test.tsx settings-page.test.tsx`
- [ ] `cd frontend && npm run build`
</verification>

<success_criteria>
- Chunk Inspector rows expose `Inspect evidence`
- Query and Chunk use the same shared evidence viewer
- Missing graph/reranker/parser/quality/link context is explicit and honest
- Viewer links route to existing Studio surfaces or clearly say why unavailable
- Runtime trust panel and evidence drawer meet keyboard/mobile accessibility requirements
- Targeted Phase 6 tests and frontend build pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-app-evidence-viewer-and-runtime-trust-banner/06-03-SUMMARY.md`
</output>
