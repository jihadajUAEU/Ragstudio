---
phase: 02-replay-and-export-tooling
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/tests/test_proof_packet_validator.py
  - backend/tests/test_proof_packet_contract.py
  - backend/src/ragstudio/proof_packet/cli.py
  - backend/src/ragstudio/proof_packet/errors.py
  - backend/src/ragstudio/proof_packet/manifest.py
  - backend/src/ragstudio/proof_packet/redaction.py
  - backend/src/ragstudio/proof_packet/validator.py
  - docs/benchmarks/ragstudio-oss-proof-v1/docs/ERRORS.md
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-07
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-09: detailed diagnostics through verbose mode; D-10: real blockers fail; D-11: advisory issues may warn; D-12: --strict treats warnings as failures."
    - "Replay/export tests cover the happy path and representative validation failures, including CLI exit codes and JSON shape."
    - "Failure fixtures mutate temp packet copies only and never corrupt the canonical proof packet."
    - "Errors remain documented and suitable for Phase 3 site-import rejection."
  artifacts:
    - path: backend/tests/test_proof_packet_validator.py
      provides: Phase 2 replay/export failure matrix
      contains: "tmp_path"
    - path: docs/benchmarks/ragstudio-oss-proof-v1/docs/ERRORS.md
      provides: Recovery guidance for hardened failures
      contains: "EXPORT_MANIFEST_INVALID"
    - path: backend/src/ragstudio/proof_packet/cli.py
      provides: Strict and verbose command behavior
      contains: "--verbose"
  key_links:
    - from: backend/tests/test_proof_packet_validator.py
      to: backend/src/ragstudio/proof_packet/validator.py
      via: validator API failure coverage
      pattern: "validate_packet"
    - from: backend/tests/test_proof_packet_validator.py
      to: scripts/proof.sh
      via: subprocess command coverage
      pattern: "proof.sh"
---

<objective>
Harden replay/export validation with representative failure-path tests and final
contract alignment.

Purpose: Make Phase 2 trustworthy enough to become the gate for Phase 3 import
work by proving success, failure, strict, verbose, JSON, and docs behavior.
Output: broad focused pytest coverage, any required validator hardening, and
final error-doc alignment.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-replay-and-export-tooling/02-CONTEXT.md
@.planning/phases/02-replay-and-export-tooling/02-RESEARCH.md
@.planning/phases/02-replay-and-export-tooling/02-PATTERNS.md
@.planning/phases/02-replay-and-export-tooling/02-VALIDATION.md
@.planning/phases/02-replay-and-export-tooling/02-01-PLAN.md
@.planning/phases/02-replay-and-export-tooling/02-02-PLAN.md
@backend/tests/test_proof_packet_contract.py
</context>

<threat_model>
Assets: validation failure coverage, CLI exit-code contract, JSON output shape,
canonical packet integrity, strict-mode CI behavior, and recovery documentation.
Threats: tests only checking the golden packet, temp mutations leaking into the
canonical packet, warnings not failing under strict mode, JSON output polluted by
human text, verbose mode masking compact contract drift, export-manifest failures
undocumented, and Phase 3 inheriting ambiguous failure codes.
Mitigations: temp packet copy fixtures, one mutation per test, subprocess CLI
tests, JSON parsing assertions, strict warning fixtures, docs/code error-code
cross-checks, and final focused pytest plus ruff.
Residual risk: full end-to-end site import remains Phase 3.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add temp packet mutation fixtures</name>
  <files>backend/tests/test_proof_packet_validator.py</files>
  <read_first>backend/tests/test_proof_packet_validator.py, backend/tests/test_proof_packet_contract.py, docs/benchmarks/ragstudio-oss-proof-v1/manifest.json</read_first>
  <action>Add helper fixtures that copy `docs/benchmarks/ragstudio-oss-proof-v1/` into `tmp_path` and provide small JSON mutation helpers. Ensure every destructive/failure test operates on the copied packet root and never writes into the canonical packet directory.</action>
  <verify>.venv/bin/python -m pytest backend/tests/test_proof_packet_validator.py -q</verify>
  <acceptance_criteria>
    - Failure tests use `tmp_path` packet copies
    - No test mutates `docs/benchmarks/ragstudio-oss-proof-v1/`
    - Test helpers support JSON update and file delete/hash mutation cases
  </acceptance_criteria>
  <done>Failure tests have safe packet mutation infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Cover representative validator failures</name>
  <files>backend/tests/test_proof_packet_validator.py, backend/src/ragstudio/proof_packet/errors.py, backend/src/ragstudio/proof_packet/manifest.py, backend/src/ragstudio/proof_packet/redaction.py, backend/src/ragstudio/proof_packet/validator.py</files>
  <read_first>.planning/phases/02-replay-and-export-tooling/02-VALIDATION.md, backend/src/ragstudio/proof_packet/errors.py, backend/src/ragstudio/proof_packet/validator.py</read_first>
  <action>Add or finish tests for `PACKET_NOT_FOUND`, `JSON_PARSE_ERROR`, `SCHEMA_INVALID`, `MANIFEST_PATH_MISSING`, `HASH_MISMATCH`, `REDACTION_LEAK`, `CLAIM_EVIDENCE_INVALID`, `CLAIM_COUNTS_MISMATCH`, `SCREENSHOT_SIGNOFF_INVALID`, `STALE_SOURCE_COMMIT`, and `EXPORT_MANIFEST_INVALID`. Harden validator behavior until each test fails closed with the expected code.</action>
  <verify>.venv/bin/python -m pytest backend/tests/test_proof_packet_contract.py backend/tests/test_proof_packet_validator.py -q</verify>
  <acceptance_criteria>
    - Every stable error code has at least one focused test or explicit docs-only rationale
    - Each representative invalid packet returns the expected code
    - The valid default packet remains green
  </acceptance_criteria>
  <done>Common validation failure paths are covered and hardened.</done>
</task>

<task type="auto">
  <name>Task 3: Cover CLI exit codes, JSON, strict, and verbose modes</name>
  <files>backend/tests/test_proof_packet_validator.py, backend/src/ragstudio/proof_packet/cli.py</files>
  <read_first>backend/src/ragstudio/proof_packet/cli.py, scripts/proof.sh, docs/benchmarks/ragstudio-oss-proof-v1/schemas/validation-result.schema.json</read_first>
  <action>Add subprocess tests for no-arg success, `--packet` success, compact `--json` parseability, invalid packet nonzero exit, `--strict` warning failure behavior, and `--verbose` detail behavior. Ensure compact JSON emits no human text before or after the JSON object.</action>
  <verify>.venv/bin/python -m pytest backend/tests/test_proof_packet_validator.py -q && ./scripts/proof.sh --strict --json | .venv/bin/python -m json.tool >/dev/null</verify>
  <acceptance_criteria>
    - No-arg `./scripts/proof.sh` exits 0 for the default packet
    - `--json` output parses as JSON and contains stable status/errors/warnings fields
    - Invalid packet subprocess exits nonzero
    - Strict mode turns warning-only validation into nonzero exit
    - Verbose mode does not change compact JSON unless requested
  </acceptance_criteria>
  <done>CLI contract is tested across success, failure, strict, JSON, and verbose modes.</done>
</task>

<task type="auto">
  <name>Task 4: Align error docs and final validation commands</name>
  <files>docs/benchmarks/ragstudio-oss-proof-v1/docs/ERRORS.md, backend/tests/test_proof_packet_validator.py, backend/tests/test_proof_packet_contract.py</files>
  <read_first>backend/src/ragstudio/proof_packet/errors.py, docs/benchmarks/ragstudio-oss-proof-v1/docs/ERRORS.md, .planning/phases/02-replay-and-export-tooling/02-VALIDATION.md</read_first>
  <action>Add a test or assertion that `ERRORS.md` documents the stable error codes exported by `errors.py`. Run focused pytest, CLI JSON validation, and ruff. If implementation results reveal a missing docs recovery note, update ERRORS.md with concrete recovery guidance.</action>
  <verify>.venv/bin/python -m pytest backend/tests/test_proof_packet_contract.py backend/tests/test_proof_packet_validator.py -q && ./scripts/proof.sh --strict --json | .venv/bin/python -m json.tool >/dev/null && .venv/bin/python -m ruff check backend/src/ragstudio/proof_packet backend/tests/test_proof_packet_validator.py backend/tests/test_proof_packet_contract.py</verify>
  <acceptance_criteria>
    - ERRORS.md documents all stable error codes
    - Focused proof packet tests pass
    - `./scripts/proof.sh --strict --json` emits parseable JSON
    - Ruff passes for proof packet code and focused tests
  </acceptance_criteria>
  <done>Phase 2 replay/export tooling is hardened and ready for verification.</done>
</task>

</tasks>

<verification>
- [ ] `.venv/bin/python -m pytest backend/tests/test_proof_packet_contract.py backend/tests/test_proof_packet_validator.py -q`
- [ ] `./scripts/proof.sh`
- [ ] `./scripts/proof.sh --strict --json | .venv/bin/python -m json.tool >/dev/null`
- [ ] `.venv/bin/python -m ruff check backend/src/ragstudio/proof_packet backend/tests/test_proof_packet_validator.py backend/tests/test_proof_packet_contract.py`
- [ ] `rg 'PACKET_NOT_FOUND|JSON_PARSE_ERROR|SCHEMA_INVALID|MANIFEST_PATH_MISSING|HASH_MISMATCH|REDACTION_LEAK|CLAIM_EVIDENCE_INVALID|CLAIM_COUNTS_MISMATCH|SCREENSHOT_SIGNOFF_INVALID|STALE_SOURCE_COMMIT|EXPORT_MANIFEST_INVALID' docs/benchmarks/ragstudio-oss-proof-v1/docs/ERRORS.md`
</verification>

<success_criteria>
- All tasks completed
- Success path and representative failure paths are covered
- CLI exit codes, compact JSON, strict mode, and verbose mode are covered
- Error docs match the exported code set
- Focused pytest, CLI JSON validation, and ruff pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-replay-and-export-tooling/02-03-SUMMARY.md`
</output>
